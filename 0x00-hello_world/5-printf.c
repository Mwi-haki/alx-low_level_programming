#include <stdio.h>

/**
 * main - prints the string in the printf function
 *
 * Description: The string is printed to the stdout
 *
 * Return: 0 if success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
